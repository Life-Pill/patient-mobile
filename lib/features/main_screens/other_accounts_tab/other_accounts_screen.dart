import 'package:flutter/material.dart';

class OtherAccountsScreen extends StatelessWidget {
  const OtherAccountsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Other Accounts Screen'),
      ),);
  }
}